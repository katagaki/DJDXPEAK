#!/bin/bash
# dab — one entry point for the whole digit-detector loop, so every step is a
# single stable command (allowlist `Training/scripts/dab` once, no per-call prompts).
# Activates the Training venv, then dispatches:
#
#   Training/scripts/dab status
#   Training/scripts/dab sheet -n 10 --out /tmp/b.png
#   Training/scripts/dab zoom --names IMG_x__5.jpg IMG_x__6.jpg --boxes --out /tmp/v.png
#   Training/scripts/dab apply /tmp/assign.json
#   Training/scripts/dab prepare                 # prepare_dataset.py --target digits
#   Training/scripts/dab train  [--epochs 120]   # train_detector.py  --target digits
#   Training/scripts/dab export [--digits-name DJDXDigitsDetector-stg-200]
#
set -euo pipefail
HERE="$(cd "$(dirname "$0")" && pwd)"
TRAINING="$(cd "$HERE/.." && pwd)"
# shellcheck disable=SC1091
source "$TRAINING/.venv/bin/activate"

cmd="${1:-status}"; shift || true
case "$cmd" in
  prepare) exec python3 "$HERE/prepare_dataset.py" --target digits "$@" ;;
  train)   exec python3 "$HERE/train_detector.py"  --target digits "$@" ;;
  export)  exec python3 "$HERE/export_coreml.py"   --only   digits "$@" ;;
  # digit_autobox subcommands (sheet/zoom/apply/status/...) — mute lib stderr so JSON stays clean
  *)       exec python3 "$HERE/digit_autobox.py" "$cmd" "$@" 2>/dev/null ;;
esac
