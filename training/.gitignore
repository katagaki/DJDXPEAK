.venv/
__pycache__/
*.pyc
.DS_Store

# uv puts caches here when used outside the project
.uv-cache/

# Compiled OCR helper binary (re-built from ocr_helper.swift by _ocr.py)
.cache/

# Generated artefacts — re-derivable from labels + scripts
dataset/
models/
output/
labels/auto/

# Hand-curated; commit these
!labels/labelstudio_export.json
!labels/label_studio_config.xml

# Classifier crops — sortable from labels, large to commit
rank_classifier_data/
clear_type_data/

# Training run noise
runs/
*.pt
